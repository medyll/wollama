// The app is a client-side SPA: it reads the local database and talks to the
// server at runtime, so there is nothing to render ahead of time.
export const prerender = false;
export const ssr = false;
