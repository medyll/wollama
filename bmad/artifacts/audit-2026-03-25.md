# Audit Report — 2026-03-25

## Summary

| Category | Status | Count |
|----------|--------|-------|
| TypeScript errors | ❌ | 1 |
| Test failures | ❌ | 21 failed / 234 total |
| console.log (non-test) | ⚠️ | ~18 in src/ |
| TODO/FIXME | ⚠️ | 6 |
| ESLint | ✅ | N/A (path issue) |

---

## 1. TypeScript Error

**File:** `tsconfig.json:11`
```
error TS5095: Option 'bundler' can only be used when 'module' is set to 'preserve' or to 'es2015' or later.
```
Fix: set `"module": "preserve"` (or `"esnext"`) in `tsconfig.json`.

---

## 2. Test Failures — 21 failing in 3 files

### Primary failure: `companion.service.test.ts` (all 21)
**Root cause:** `DataGenericService` is mocked as a factory function, but `CompanionService` instantiates it with `new`. The mock returns a plain function instead of a constructor, so `new DataGenericService(...)` throws `TypeError: ... is not a constructor`.

Fix: replace `vi.mock` factory with `vi.fn().mockImplementation(...)` that returns a class-like mock, or use `vi.spyOn` on the constructor.

### Other failing test files
- `CompanionEditor.test.ts` — skipped (TODO: SSR vs DOM env)
- `CompanionSelector.test.ts` — skipped (TODO: SSR vs DOM env)
- `onboarding/*.test.ts` — 3 files skipped (same TODO)

---

## 3. console.log — Debug statements in production code

Key files with cleanup needed:

| File | Count |
|------|-------|
| `src/lib/services/sync.service.ts` | 6 |
| `src/lib/db.ts` | 4 |
| `src/lib/services/chat.service.ts` | 3 |
| `src/routes/+layout.svelte` | 1 |
| `src/lib/auth.ts` | 1 |
| `src/lib/services/data-initializer.ts` | 1 |
| `src/lib/state/connection.svelte.ts` | 2 |

`_old/` components are pre-existing legacy — lower priority.

Recommendation: replace with a debug logger (e.g. `import.meta.env.DEV && console.log(...)`) or remove before S6 polish sprint.

---

## 4. TODOs

| File | Note |
|------|------|
| `src/components/chat/MessageActions.svelte:28` | Rating persistence not implemented |
| `src/components/CompanionEditor.test.ts:19` | Test disabled — SSR/DOM conflict |
| `src/components/CompanionSelector.test.ts:38` | Test disabled — SSR/DOM conflict |
| `src/routes/onboarding/onboarding.test.ts:25` | Test disabled |
| `src/routes/onboarding/story-1-2.test.ts:38` | Test disabled |
| `src/routes/onboarding/story-2-1.test.ts:5` | Test disabled |

---

## Action Items (Priority Order)

1. **[HIGH]** Fix `companion.service.test.ts` mock — `DataGenericService` constructor mock
2. **[HIGH]** Fix `tsconfig.json` `bundler` module error
3. **[MED]** Strip `console.log` from production code before S6
4. **[LOW]** Re-enable skipped Svelte component tests (SSR environment issue)
5. **[LOW]** Implement `MessageActions` rating persistence (backlog item)
