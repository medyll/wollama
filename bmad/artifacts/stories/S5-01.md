S5-01 — Wire ToolCallMessage into chat message list

Goal
Integrate the existing ToolCallMessage.svelte component into the chat message list so that messages with type 'tool_call' render the agent status UI instead of a standard bubble.

Tasks
- Find the chat message list component (likely client/src/lib/components/ or client/src/routes/)
- Add a conditional branch: if message.role === 'tool' AND message.tool_call_id, render <ToolCallMessage>
- Pass tool_call_id and slug (from message.skill_invoked or metadata) as props
- Unit test: message with tool_call_id renders ToolCallMessage, normal message does not

Acceptance
- A message with role=tool and tool_call_id renders ToolCallMessage
- Normal user/assistant messages are unaffected
- Test covers both branches
